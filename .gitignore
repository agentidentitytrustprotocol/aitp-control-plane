/node_modules
/.next
/out
/build
/coverage
/.turbo
.DS_Store
*.tsbuildinfo
next-env.d.ts
.env
.env.local
.env.*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

temp/
plans/*
# Cross-repo plans are tracked: written here, but read by a sibling repo, so a
# GitHub blob URL has to resolve from that repo's issues and PRs. Local working
# plans stay ignored.
!plans/cross-repo/
